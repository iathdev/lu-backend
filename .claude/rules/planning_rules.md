# Planning Rules

When creating a plan (via `/plan` or plan mode), **always present at least 2 alternatives**. For each plan:
1. Describe each alternative approach clearly
2. Compare pros/cons between them (e.g., complexity, performance, maintainability, alignment with current architecture)
3. Recommend one with justification, but let the user decide
